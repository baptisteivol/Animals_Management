server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /var/www/Animals_app;
        server_name _;
        location / {
                try_files $uri $uri/ =404;
        }
}
